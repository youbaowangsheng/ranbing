# FIPAI Agent 平台核心模块设计文档

> 版本：v1.0 · 2026年5月3日
> 状态：已完成全部6个核心模块HTML原型

---

## 一、整体架构

FIPAI Agent平台分为三层：

```
┌─────────────────────────────────────────────────────┐
│                  构建工具层（业务人员）                │
│  创建向导 → Prompt编辑器 / 技能市场 / 工作流编排器      │
│  → 触发配置 → 监控与日志 → 文档中心                    │
└─────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────┐
│                 核心运行时层（系统自动运行）              │
│  意图识别 → 调度路由 → 多Agent协作 → 记忆积累           │
│  → 反馈闭环 → 自动优化                                  │
└─────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────┐
│                  工程保障层（运维/管理员）              │
│  沙箱测试 → 版本管理 → 权限控制 → 预算管控 → 审计日志    │
└─────────────────────────────────────────────────────┘
```

---

## 二、核心模块详细设计

### 模块A：Agent 调度与路由

**定位**：用户请求进入系统后的第一道关卡 — 判断"谁来处理"

**核心功能**

1. **意图识别引擎**
   - LLM驱动，输入用户消息，输出：意图类型 + 置信度 + 实体提取
   - 内置5种意图类型：找人合作、发布供需、供需匹配、闲聊问答、社群互动
   - 每种意图可配置关键词 + 默认Agent + 置信度阈值
   - 支持切换底层模型：DeepSeek V3（推荐） / GPT-4o Mini（更准） / DeepSeek R1（复杂推理）

2. **路由分发策略**
   - 优先级匹配（默认）：优先匹配置信度最高的Agent
   - 多Agent并行：同时分发给多个Agent，返回综合结果（成本x2）
   - 置信度兜底：低于阈值时转人工客服

3. **Agent 注册表**
   - 每个Agent声明自己能处理哪些意图 + 优先级
   - 未启用的Agent不参与路由
   - 优先级决定多个Agent同时匹配时的选择顺序

4. **路由模拟测试**
   - 输入任意用户消息，实时显示：意图识别结果 → 路由匹配结果 → 分发Agent
   - 无需真正执行Agent即可验证路由逻辑

**数据流**

```
用户消息
  ↓
意图识别（LLM分类）→ {意图类型, 置信度, 实体}
  ↓
路由匹配（查询Agent注册表，按优先级/置信度筛选）
  ↓
分发到对应的Agent执行
  ↓
返回结果
```

**关键配置项**

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| 意图识别模型 | 影响识别准确率和成本 | DeepSeek V3 |
| 置信度阈值 | 低于此值触发兜底策略 | 0.4 |
| 路由策略 | 优先级/并行/置信度兜底 | 优先级匹配 |
| 兜底目标 | 低于阈值的请求发往 | 人工客服 |

---

### 模块B：Agent 记忆与上下文

**定位**：让Agent"越用越懂用户"，跨会话积累知识

**三层记忆架构**

```
┌──────────────────────────────────────────────────────┐
│  👤 用户Profile（结构化存储，持续更新）                  │
│  从对话中自动提取的标签化用户特征                         │
│  · 行业标签（制造业、金融、科技）                         │
│  · 需求标签（找投资人、招聘、渠道）                       │
│  · 合作偏好（股权合作、项目合作）                         │
│  · 活跃度、社群、互动历史                               │
└──────────────────────────────────────────────────────┘
                        ↑
┌──────────────────────────────────────────────────────┐
│  📦 长期记忆（向量数据库，永久存储）                      │
│  跨会话积累的重要信息，供检索                            │
│  · 用户历次发布的供需记录                               │
│  · 成功撮合的合作案例                                   │
│  · 用户反馈过的偏好                                     │
│  · 社群中的互动历史                                     │
└──────────────────────────────────────────────────────┘
                        ↑
┌──────────────────────────────────────────────────────┐
│  ⚡ 短期记忆（Session，会话期间有效）                     │
│  当前会话内的上下文，Agent能"看到"对话历史                │
│  · 当前对话的所有消息                                   │
│  · 用户当前的问题/需求                                  │
│  · Agent刚才给出的回答                                 │
│  · 正在进行的多轮对话状态                               │
└──────────────────────────────────────────────────────┘
```

**记忆积累示例（李明的故事）**

```
时间线：

10:23  李明：帮我看看有没有适合做工厂数字化的合作方
       ↓ 存入短期记忆 ✓
       ↓ 存入长期记忆 ✓（供需记录）
       ↓ 提取标签：需求=找工厂渠道 ✓

10:23  Agent：找到了3个匹配，①王强...
       ↓ 更新Profile：需求=找工厂渠道

10:25  李明：王强可以，我想要他的联系方式
       ↓ 短期记忆：主动发起牵线

10:27  系统：牵线成功！王强同意了
       ↓ 写入Profile：合作成功+1
       ↓ 存入案例库

10:30  李明：这个合作方质量很高，感谢！
       ↓ 用户反馈：正向
       ↓ 强化标签：数字化/制造业

下次李明来的时候，Agent看到他的Profile，
直接说："李明，我们刚上线了一个工厂数字化社群，
里面有7家大型制造企业的高管，你要不要看看？"
```

**记忆规则配置**

| 记忆类型 | 存储方式 | 配置项 |
|---------|---------|--------|
| 短期记忆 | 会话期间 | 保留最近N轮对话（建议10-20轮） |
| 长期记忆 | 向量数据库 | 可配置存入条件（需求/供给/成功案例/重要标记） |
| Profile | 结构化DB | 自动提取行业/需求/偏好标签 |

---

### 模块C：多Agent协作编排

**定位**：复杂任务需要多个Agent配合，通过可视化流程串联

**为什么需要多Agent协作**

- 单Agent的Prompt臃肿后效果不稳定
- 每个Agent专注一个角色，通过标准接口传递数据
- 可组合、可复用、可调试

**4种协作模式**

| 模式 | 适用场景 | 示意 |
|------|---------|------|
| 顺序执行 | 步骤强依赖，上一步输出是下一步输入 | A → B → C → 结果 |
| 并行执行 | 多个独立子任务，同时完成再汇总 | A｜B｜C 同时 → 汇总 → 结果 |
| 条件分支 | 根据中间结果判断走哪个分支 | A → 判断 → B1路径 或 B2路径 |
| 混合模式 | 顺序+并行+条件分支组合 | A → [B｜C并行] → D判断 → E/F |

**典型流程示例：机会发现 → 人脉匹配 → 推送**

```
👤 用户发起请求
    ↓ user_request
📝 信号发现（Agent-A）→ 发现3条供需
    ↓ matched_demands[]
    ├→ 🤝 人脉匹配（Agent-B）→ 找到5位相关校友
    │       ↓ matched_users[]
    └→ 📊 话术生成（Agent-C）→ 生成3条推荐话术
            ↓ outreach_scripts[]
📢 推送执行（Agent-D）→ 发送个性化通知
    ↓ notifications[]
✅ 流程完成
```

**预置协作模板**

1. 机会发现 → 人脉匹配 → 推送（信号发现+匹配+话术+执行）
2. AI牵线全流程（话术生成→确认→发送→交换联系方式）
3. 每日摘要生成（数据采集→分析→摘要→推送）
4. 新用户引导（欢迎→兴趣探测→推荐社群→介绍人脉）

---

### 模块D：沙箱测试与版本管理

**定位**：Agent上线前的安全阀 + 上线后的版本控制

**三大功能**

1. **沙箱测试**
   - 隔离测试环境，不影响线上
   - 可配置Agent版本、测试输入、模拟上下文（用户Profile、历史记录）
   - 实时显示Token消耗和响应时间
   - 每次测试自动清理

2. **版本历史**
   - 每次保存生成一个新版本（v2.3, v2.2...）
   - 记录变更描述和变更标签（Prompt优化/Bug修复/新增功能）
   - 支持回滚到任意历史版本
   - 草稿状态：未保存的修改

3. **灰度发布**
   - 可配置流量分配（10%新版本/90%旧版本）
   - 设置自动提升条件（如：连续7天匹配准确率>90%）
   - 满足条件后自动将新版本提升至100%

4. **A/B测试**
   - 同时运行两个版本，对比：匹配准确率、测试样本数、平均响应时间
   - 实时显示流量占比和胜出状态
   - 支持一键采用胜出版本

---

### 模块E：反馈闭环与自动优化

**定位**：用户每次点"有帮助/没帮助"，都是Agent进化的机会

**反馈闭环流程**

```
┌─ 正向路径 ─────────────────────────────────────────┐
用户收到结果 → 👍 → 记录反馈 → 强化学习 → 优化Prompt  │
│  5次正向 → 标记"优质响应"，优先复用                 │
└────────────────────────────────────────────────────┘

┌─ 负向路径 ─────────────────────────────────────────┐
用户收到结果 → 👎 → 记录反馈 → 根因分析 → 调整路由    │
│  3次负向 → 生成优化建议报告                        │
└────────────────────────────────────────────────────┘
```

**自动优化规则**

| 规则 | 触发条件 | 自动动作 |
|------|---------|---------|
| 正向反馈自动强化 | 同一输出获得5次以上正向反馈 | 标记为"优质响应"，优先复用 |
| 负向反馈自动分析 | 同一输出收到3次负向反馈 | 触发根因分析，生成优化建议 |
| 路由策略自动调整 | 某类意图准确率持续低于70% | 降低置信度阈值，提示人工审核 |
| 周报自动生成 | 每周一 | 生成上周反馈分析周报 |

**周报内容**

- 正向率趋势（每日柱状图）
- 负向反馈Top原因（条形图）
- 优化建议（按优先级：高/中/低）

---

### 模块F：企业级权限与预算

**定位**：多用户、多团队场景下的资源管控和安全合规

**预算管理**

- 月度总预算 + 日预算上限
- 实时使用进度条 + 预测月底消耗
- 预警阈值（默认80%，触发提醒）
- 超预算自动暂停开关
- 按Agent分解消耗 + 按用户排行
- 每日限额监控（单个Agent日消耗超限预警）

**权限矩阵**

| 操作 | 管理员 | 开发者 | 运营 | 查看者 |
|------|--------|--------|------|--------|
| 创建Agent | ✓ | ✓ | ✓ | — |
| 编辑Agent | ✓ | ✓ | ✓ | — |
| 删除Agent | ✓ | — | — | — |
| 查看调用日志 | ✓ | ✓ | ✓ | ✓ |
| 配置预算 | ✓ | — | — | — |
| 管理成员 | ✓ | — | — | — |
| 发布Agent | ✓ | ✓ | — | — |
| 查看审计日志 | ✓ | — | — | — |

**审计日志**

- 记录所有关键操作：谁、什么时候、做了什么、改了什么
- 支持按操作类型筛选（Agent管理/权限管理/预算配置）
- 支持按成员筛选
- 可导出报表

---

## 三、模块间数据流

```
用户请求
    ↓
[模块A] 意图识别 → 路由分发
    ↓
[模块B] 加载用户Profile + 短期/长期记忆
    ↓
[模块C] 多Agent协作执行
    ├── Agent-A（信号发现）
    ├── Agent-B（人脉匹配）
    ├── Agent-C（话术生成）
    └── Agent-D（推送执行）
    ↓
[模块B] 更新记忆（存入新交互记录）
    ↓
用户收到结果 → 点"有帮助/没帮助"
    ↓
[模块E] 反馈收集 → 自动优化规则判断
    ├── 正向强化 → 更新Prompt权重
    └── 负向分析 → 生成优化建议 → 回馈到[模块D]版本管理
    ↓
[模块D] 沙箱测试验证 → 新版本发布
    ↓
[模块F] 预算检查 + 权限校验 + 审计记录
```

---

## 四、HTML原型文件清单

| 文件 | 名称 | 大小 |
|------|------|------|
| core_pages_index.html | 核心模块总览 | ~2KB |
| module_A_agent_router.html | Agent调度与路由 | ~27KB |
| module_B_agent_memory.html | Agent记忆与上下文 | ~23KB |
| module_C_multi_agent.html | 多Agent协作编排 | ~19KB |
| module_D_sandbox.html | 沙箱测试与版本管理 | ~20KB |
| module_E_feedback.html | 反馈闭环与自动优化 | ~22KB |
| module_F_enterprise.html | 企业级权限与预算 | ~23KB |

路径：`/Users/wangsheng/fipai/frontend/admin/src/pages/agent_builder/core_pages/`

---

## 五、与构建工具层的关系

构建工具层（业务人员用）配置好的Agent，由核心运行时层（系统）来调度执行：

```
构建工具层                          核心运行时层
─────────────────────────────────────────────────────
module_01 创建向导                   module_A 调度与路由
  ↓ 选择模式                            ↓
module_02 Prompt编辑器  ───────────→  Intent Router（用Prompt做意图识别）
  或                                    ↓
module_03 技能市场  ───────────────→  Agent Executor
  或                                      ↓
module_04 工作流编排器  ───────────→  module_B 记忆（上下文注入）
                                      ↓
module_05 触发配置  ───────────────→  module_E 反馈（结果收集）
                                      ↓
module_06 监控日志  ───────────────→  module_D 版本管理（优化闭环）
```

---

## 六、上线优先级建议

**第一期（MVP，必须）**
- module_A（调度路由）+ module_B（记忆）— 这两个是核心，没有则平台无法运行
- module_D（沙箱测试）— 上线前必须能测试，否则线上事故概率高

**第二期（核心体验）**
- module_C（多Agent协作）— FIPAI的核心卖点，机会发现→匹配→推送必须走这条路
- module_E（反馈闭环）— 让AI越用越准，用户留存关键

**第三期（企业级）**
- module_F（权限与预算）— 多团队运营后必须，否则成本不可控
