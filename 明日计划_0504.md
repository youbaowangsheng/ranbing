# 明日计划 · 2026年5月4日（周一）

> 目标：完成燃冰MVP的数据模型设计 + API接口定义

---

## 一、数据模型设计（上午）

### 1.1 数据库Schema设计

设计以下表结构（PostgreSQL）：

```
users          — 用户表（手机号/微信openid）
profiles       — 档案表（关联users，基础信息+认证等级）
tags           — 标签表（L1~L5五层）
profile_tags   — 用户-标签关联表（用户选了哪些标签）
supplies       — 供需表（类型/内容/标签/状态/发布时间）
matches        — AI匹配记录表（供需对/匹配分/状态/AI理由）
connections    — 关系链表（user_a/user_b/关系强度/最近互动时间）
followups      — 跟进记录表（发起人/目标人/AI话术/状态）
```

### 1.2 向量索引设计

```
profile_embeddings — 用户向量表（pgvector）
  profile_id, embedding(vector 1536), updated_at

supply_embeddings — 供需向量表
  supply_id, embedding, tags[]
```

### 1.3 输出物

- `schema.sql` — 完整建表语句（含索引）
- `数据模型设计.md` — 每个表的字段说明、关系图、读写频率分析

---

## 二、API接口设计（下午）

### 2.1 接口清单（MVP范围）

**认证相关**
```
POST /api/auth/login          — 手机号+验证码登录
POST /api/auth/register       — 注册
GET  /api/auth/profile        — 获取当前用户Profile
```

**Profile相关**
```
GET  /api/profiles/:id        — 查看他人Profile
PUT  /api/profiles/me         — 编辑我的Profile
POST /api/profiles/me/cert    — 提交认证
```

**标签相关**
```
GET  /api/tags                — 获取全部标签（L1~L5）
POST /api/profiles/me/tags    — 更新我的标签
```

**供需相关**
```
GET  /api/supplies            — 供需广场列表（分页+标签筛选）
POST /api/supplies            — 发布供需
GET  /api/supplies/feed       — AI推荐Feed（我的标签匹配）
```

**AI相关**
```
POST /api/ai/recognize-intent — 意图识别（文本→意图类型）
POST /api/ai/extract-tags     — 标签提取（文本→标签列表）
POST /api/ai/match            — AI匹配（供需对→匹配度+理由）
POST /api/ai/generate-script  — AI生成话术（牵线话术/跟进话术）
```

**人脉相关**
```
GET  /api/connections         — 我的关系链
POST /api/connections         — 建立连接（发牵线请求）
PUT  /api/followups/:id       — 更新跟进状态
```

### 2.2 输出物

- `api_spec.md` — 每个接口的请求/响应格式、错误码
- 可用Postman/Apifox导入格式

---

## 三、明日交付物清单

| 交付物 | 文件名 | 说明 |
|--------|--------|------|
| 数据库Schema | `schema.sql` | PostgreSQL建表语句 |
| 数据模型文档 | `数据模型设计.md` | 表结构+关系+索引 |
| API接口文档 | `api_spec.md` | 完整接口定义 |

---

## 四、执行步骤

```
09:00 ~ 12:00  数据模型设计
  ├── 确定表结构
  ├── 确定字段类型和约束
  ├── 设计索引
  └── 输出 schema.sql + 数据模型设计.md

14:00 ~ 18:00  API接口设计
  ├── 梳理每个接口的业务逻辑
  ├── 确定请求/响应格式
  ├── 错误码设计
  └── 输出 api_spec.md
```

---

## 五、依赖与风险

| 依赖/风险 | 说明 | 应对 |
|-----------|------|------|
| 微信登录 | 需微信公众平台AppID | 先用手机号+验证码代替 |
| DeepSeek API | 需申请API Key | 先本地Mock，等申请通过 |
| pgvector扩展 | 需确认DB版本支持 | PostgreSQL 15+自带，如不支持用Faiss替代 |
| 前端技术栈 | 还未确定小程序/Web框架 | API设计为RESTful，兼容任意前端 |
