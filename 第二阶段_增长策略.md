# 燃冰 · 第二阶段：增长策略

> 时间：MVP上线后 4~6 周
> 目标：提升用户活跃度，让关系数据沉淀，产生网络效应

---

## 一、阶段目标

MVP阶段解决了"有没有"的问题，第二阶段要解决"用不用"的问题。

**核心指标：**
- DAU/MAU 比值（目标 > 30%）
- 人均供需发布数（目标 > 3 条/人）
- 匹配后发起联系率（目标 > 25%）
- 关系链沉淀数（目标 > 5 条/人）

---

## 二、功能 roadmap

### 2.1 活动系统（4~5个功能点）

**L. 活动列表页（新增）**
- AI个性化推荐（根据用户标签推荐相关活动）
- 分类筛选（行业/职能/规模）
- 往期回顾（参加过/感兴趣的活动）

**M. 活动详情页（新增）**
- AI推荐参与人（"根据你的供需，以下3人值得认识"）
- 已报名头像墙（展示校友圈深度）
- 合作轨迹展示（推荐的人和你的历史供需关系）
- 一键报名 + 进入活动专属群

**活动前AI邀约**
- 根据供需匹配度，主动给潜在感兴趣的用户发推送
- "下周有个芯片行业沙龙，有3位校友也在，你认识吗？"

**活动后AI跟进（R页面增强）**
- 活动结束后自动生成待跟进名单
- AI根据活动中互动情况排序跟进优先级
- 一键生成个性化话术

### 2.2 社群系统（3个功能点）

**N_v2. 社群页增强**
- 我的社群（已加入的社群列表）
- AI精选推荐（"根据你的供需，这些社群可能有用"）
- 发现更多社群

**N_v2. 机会Tab（新增）**
- AI实时监听群聊商业信号
- 三类信号卡片：供需信号 / 问答信号 / 合作信号
- 信号匹配推送（推送给相关供需的校友）

**AI穿针引线**
- 识别到匹配信号后，AI生成牵线话术
- 双方确认后交换联系方式
- 成功率追踪

### 2.3 人脉图谱（2个功能点）

**G. 关系网络图（新增）**
- 你的校友圈人脉可视化
- 一度人脉（直接认识）
- 二度人脉（朋友的朋友）——通过共同好友介绍
- 关系权重展示（互动频率/合作历史）

**人脉推荐增强**
- 基于图谱的推荐："你认识A，通过A可以认识B"
- 路径展示："刘总→张总→李总"最短路径

---

## 三、增长策略

### 3.1 冷启动策略

**种子用户（0~100人）**

问题：平台早期没有数据，AI推荐不准，用户不愿用

解法：
1. 运营人员人工导入30~50个高活跃校友的供需信息
2. 邀请5~10位KOL用户，承诺1对1运营服务
3. 内部测试期（2周），只开放给MBA校友会核心成员

### 3.2 内容填充策略

**AI辅助内容生成**

问题：早期供需信息少，Feed不丰富

解法：
1. 用户发布信息时，AI自动扩展描述（生成更完整的供需摘要）
2. AI根据Profile信息，自动为用户生成"推荐关注"标签
3. 对没有发布过供需的用户，AI生成"试探性供需"草稿，用户一键确认/修改

### 3.3 邀请裂变策略

**老带新机制**

```
老用户邀请新用户注册：
  → 老用户：解锁"人脉达人"徽章 + 优先推荐权
  → 新用户：首周获得"3次免费AI牵线"权益

老用户发布供需并成功匹配：
  → 解锁"活跃达人"标签 + 被推荐到首页概率+50%
```

### 3.4 推送召回策略

**流失用户召回**

| 用户行为 | 触发推送 | 推送内容 |
|---------|---------|---------|
| 3天未登录 | 微信服务号模板消息 | "王总，有3位新校友可能对你有帮助" |
| 7天未发布供需 | 短信 | "最近有人发布了你可能感兴趣的供需" |
| 14天未互动 | 短信 | "你的人脉网络有更新，看看谁新加入了" |

**推送频率控制**
- 每日推送上限：3条
- 同一类型推送间隔：至少4小时
- 用户可自定义接收频率

---

## 四、关键指标监控

### 4.1 AARRR漏斗

```
Acquisition（获取）
  └── 新增注册用户数（目标：每周+50）

Activation（激活）
  └── 完成认证用户比例（目标：> 60%）
  └── 首次发布供需完成率（目标：> 40%）

Retention（留存）
  └── 次日留存率（目标：> 35%）
  └── 7日留存率（目标：> 20%）

Revenue（变现）
  └── 付费转化（暂不开放，留作第三阶段）

Referral（推荐）
  └── 老带新邀请率（目标：> 15%）
  └── 人均分享次数（目标：> 0.5次/人）
```

### 4.2 AI效果指标

| 指标 | 定义 | 目标值 |
|------|------|--------|
| 意图识别准确率 | AI识别的意图与用户真实意图一致 | > 85% |
| 标签提取完整率 | AI提取的标签被用户采纳 | > 70% |
| 匹配命中率 | AI推荐的匹配被用户点击 | > 30% |
| 牵线成功率 | 发起牵线后双方建立联系 | > 40% |
| 反馈正向率 | 用户点"有帮助"比例 | > 75% |

---

## 五、技术实现要点

### 5.1 定时任务设计

```python
# 每日定时任务（Celery Beat）
celery beat schedule:

  # 每日早9点：生成今日推荐Feed
  daily_feed_generation:
    task: generate_daily_feed
    schedule: crontab(hour=9, minute=0)

  # 每30分钟：扫描新供需，计算匹配
  supply_match_scan:
    task: scan_and_match_supplies
    schedule: timedelta(minutes=30)

  # 活动前24小时：发送活动提醒
  event_reminder:
    task: send_event_reminder
    schedule: crontab(hour=10, minute=0)

  # 活动后24小时：生成跟进清单
  event_followup:
    task: generate_event_followup
    schedule: crontab(hour=10, minute=0)

  # 每周一：生成周报
  weekly_report:
    task: generate_weekly_report
    schedule: crontab(hour=9, minute=0, day_of_week=1)
```

### 5.2 微信消息推送接入

```python
# 微信服务号模板消息
def send_template_message(user_openid, template_id, data):
    """
    data格式：
    {
      "first": {"value": "王总，您有新的匹配！"},
      "keyword1": {"value": "芯片供需匹配"},
      "keyword2": {"value": "92%匹配"},
      "remark": {"value": "点击查看详情"}
    }
    """
    url = f"https://api.weixin.qq.com/cgi-bin/message/template/send"
    payload = {
        "touser": user_openid,
        "template_id": template_id,
        "data": data,
        "miniprogram": {"page": "pages/index/index"}
    }
    # 调用微信API发送
```

---

## 六、风险与应对

| 风险 | 概率 | 影响 | 应对 |
|------|------|------|------|
| 微信模板消息审核被拒 | 中 | 高 | 提前准备多个模板ID，预留2周审核时间 |
| 活动报名率低 | 中 | 中 | 先用小范围邀请制，积累口碑后再开放 |
| 群聊信号识别不准 | 高 | 中 | 第一版先做关键词匹配，AI辅助；后续升级LLM识别 |
| 用户隐私顾虑 | 低 | 高 | 明确告知数据使用规则，提供一键关闭推送 |
| KOL用户流失 | 中 | 高 | 1对1运营服务，承诺不打扰（每周最多2条推送） |

---

## 七、上线检查清单

### 功能验收

```
□ L.活动列表：AI推荐准确率 > 70%
□ M.活动详情：AI推荐参与人卡片展示正常
□ M.活动详情：一键报名功能正常
□ N_v2.社群：机会Tab信号刷新正常
□ N_v2.社群：AI穿针引线流程跑通
□ G.人脉图谱：一度/二度人脉展示正常
□ G.人脉图谱：关系权重计算正常
□ R.跟进提醒：活动后跟进清单生成正常
□ 推送：微信模板消息发送正常
□ 推送：召回消息发送正常
```

### 数据验收

```
□ 用户关系链沉淀正常
□ 匹配记录完整
□ 跟进记录可查
□ 周报生成正常
```
