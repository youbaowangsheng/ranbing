  燃冰后台运营端规划

     Context

     燃冰是商务社交平台，通过 FIPAI 提供 AI 能力。燃冰业务层独立部署 Django + DRF 后端，通过 HTTP 调用 FIPAI 的 Agent CRUD
     API。燃冰在业务层定义自己的 Agent 类型（社群助手、供需匹配助手、发布助手、匹配助手等），管理 AI Agent 实例的业务配置。

     目标：为燃冰构建完整的运营后台，包含用户管理、Agent 业务配置、运营分析、内容管理等核心模块。

     技术架构：
     [燃冰 Console] → [燃冰 Django API] → [FIPAI Django API]
                                   ↓
                         [燃冰 MySQL 数据库]

     - FIPAI：通用 Agent CRUD 接口（端口 8001）
     - 燃冰 Django API：业务逻辑独立服务
     - 燃冰 MySQL：用户、账户、业务配置等数据
     - 燃冰 Console：React 前端（端口 5173 dev）

     ---
     Phase 1: 基础架构（先做）

     1.1 燃冰 Django API 项目初始化

     目录: /Users/wangsheng/ranbing/ (新建)
     - Django + DRF 项目
     - MySQL 数据库连接
     - 连接 FIPAI API 的客户端封装
     - CORS 配置允许燃冰 Console 访问

     1.2 数据库模型

     文件: apps/core/models.py
     class UserProfile(models.Model):
         """扩展用户信息"""
         user = OneToOneField(User)
         phone = CharField
         company = CharField
         industry = CharField
         plan = CharField  # free/pro/enterprise
         max_agents = IntegerField
         token_quota = BigIntegerField
         token_used = BigIntegerField(default=0)

     class AgentConfig(models.Model):
         """Agent 业务配置（不存储在 FIPAI）"""
         user = ForeignKey(User)
         fipai_agent_id = IntegerField  # 关联 FIPAI Agent ID
         agent_type = CharField  # community/matching/publish
         config = JSONField  # 业务特定配置
         is_active = BooleanField(default=True)
         created_at = DateTimeField

     class UsageRecord(models.Model):
         """用量记录"""
         user = ForeignKey(User)
         agent = ForeignKey(AgentConfig)
         tokens = BigIntegerField
         cost = DecimalField
         created_at = DateTimeField

     1.3 燃冰 Console API 层

     文件: src/utils/ranbingApi.ts (新建)
     - 指向燃冰 Django API (端口 8002)
     - 用户管理 API
     - Agent 业务配置 API
     - 用量统计 API

     ---
     Phase 2: 用户与账户管理

     2.1 用户管理 API

     文件: apps/users/views.py
     - GET /api/users/ - 用户列表
     - GET /api/users/{id}/ - 用户详情
     - PUT /api/users/{id}/ - 更新用户
     - POST /api/users/{id}/disable - 禁用/启用

     2.2 账户管理

     文件: apps/accounts/views.py
     - GET /api/accounts/profile/ - 企业账户信息
     - PUT /api/accounts/profile/ - 更新账户
     - GET /api/accounts/usage/ - 用量统计

     2.3 Console 页面

     - src/pages/Users.tsx - 用户列表
     - src/pages/UserDetail.tsx - 用户详情
     - src/pages/Accounts.tsx - 账户管理

     ---
     Phase 3: Agent 业务配置

     3.1 Agent 配置 API

     文件: apps/agents/views.py
     - GET /api/business/agents/ - 获取用户所有 Agent 及业务配置
     - POST /api/business/agents/ - 创建 FIPAI Agent 并保存业务配置
     - PUT /api/business/agents/{id}/ - 更新业务配置
     - DELETE /api/business/agents/{id}/ - 删除（调用 FIPAI 删除 + 清理本地配置）

     3.2 Console 页面

     - src/pages/AgentBusiness.tsx - Agent 业务配置列表（区别于 FIPAI 原生管理）
     - src/pages/AgentBusinessEdit.tsx - 编辑业务配置

     ---
     Phase 4: 运营数据分析

     4.1 用量统计 API

     文件: apps/stats/views.py
     - GET /api/stats/dashboard/ - Dashboard 汇总
     - GET /api/stats/daily/?days=7 - 日趋势
     - GET /api/stats/usage/ - 用量报表（分页）

     4.2 Console 页面

     - src/pages/Dashboard.tsx - 增强现有 Dashboard
     - src/pages/UsageReport.tsx - 用量报表

     ---
     Phase 5: 业务特定功能

     5.1 社群内容管理

     - apps/community/views.py
     - src/pages/Community.tsx

     5.2 匹配队列

     - apps/matching/views.py
     - src/pages/Matching.tsx

     5.3 发布管理

     - apps/publish/views.py
     - src/pages/Publish.tsx

     ---
     关键文件清单

     ┌─────────────────────────────┬──────┬──────────────────┐
     │            文件             │ 操作 │       说明       │
     ├─────────────────────────────┼──────┼──────────────────┤
     │ /Users/wangsheng/ranbing/   │ 新建 │ 燃冰 Django 项目 │
     ├─────────────────────────────┼──────┼──────────────────┤
     │ ranbing/apps/users/         │ 新建 │ 用户管理         │
     ├─────────────────────────────┼──────┼──────────────────┤
     │ ranbing/apps/accounts/      │ 新建 │ 账户管理         │
     ├─────────────────────────────┼──────┼──────────────────┤
     │ ranbing/apps/agents/        │ 新建 │ Agent 业务配置   │
     ├─────────────────────────────┼──────┼──────────────────┤
     │ ranbing/apps/stats/         │ 新建 │ 统计报表         │
     ├─────────────────────────────┼──────┼──────────────────┤
     │ ranbing/apps/community/     │ 新建 │ 社群管理         │
     ├─────────────────────────────┼──────┼──────────────────┤
     │ ranbing/apps/matching/      │ 新建 │ 匹配队列         │
     ├─────────────────────────────┼──────┼──────────────────┤
     │ ranbing/apps/publish/       │ 新建 │ 发布管理         │
     ├─────────────────────────────┼──────┼──────────────────┤
     │ src/utils/ranbingApi.ts     │ 新建 │ 燃冰 API 客户端  │
     ├─────────────────────────────┼──────┼──────────────────┤
     │ src/pages/Users.tsx         │ 新建 │ 用户列表         │
     ├─────────────────────────────┼──────┼──────────────────┤
     │ src/pages/UserDetail.tsx    │ 新建 │ 用户详情         │
     ├─────────────────────────────┼──────┼──────────────────┤
     │ src/pages/Accounts.tsx      │ 新建 │ 账户管理         │
     ├─────────────────────────────┼──────┼──────────────────┤
     │ src/pages/AgentBusiness.tsx │ 新建 │ Agent 业务配置   │
     ├─────────────────────────────┼──────┼──────────────────┤
     │ src/pages/Dashboard.tsx     │ 增强 │ 运营 Dashboard   │
     ├─────────────────────────────┼──────┼──────────────────┤
     │ src/pages/UsageReport.tsx   │ 新建 │ 用量报表         │
     └─────────────────────────────┴──────┴──────────────────┘

     ---
     验证步骤

     1. 启动燃冰 Django API: cd /Users/wangsheng/ranbing && python manage.py runserver 8002
     2. 启动 FIPAI: cd /Users/wangsheng/fipai && python manage.py runserver 8001
     3. 启动燃冰 Console: cd /Users/wangsheng/fipai-console && npm run dev
     4. 测试流程：
       - 注册/登录
       - 创建 Agent → 自动在 FIPAI 创建 + 本地保存配置
       - Dashboard 查看运营数据
       - 用户管理

     ---
     实施顺序

     1. Phase 1 - 基础架构（Django 项目 + 模型 + API 层）
     2. Phase 2 - 用户与账户管理
     3. Phase 3 - Agent 业务配置（核心）
     4. Phase 4 - 运营数据分析
     5. Phase 5 - 业务特定功能

     Context

     燃冰是一个 B2B SaaS 平台，通过 FIPAI 提供 AI 能力。燃冰在业务层定义自己的 Agent
     类型（社群助手、供需匹配助手、发布助手、匹配助手等），通过 FIPAI 的通用 Agent CRUD API 管理 AI Agent 实例。

     目标：为燃冰构建完整的运营后台，包含用户管理、Agent 实例管理、运营分析、内容管理、配置管理等核心模块。

     FIPAI 职责：仅提供通用 Agent CRUD 接口（已实现）

     燃冰 Console 职责：业务层定制，包括：
     - 用户/账户管理
     - Agent 实例的业务配置（不是 CRUD，而是业务属性）
     - 运营数据分析
     - 业务特定功能（社群内容、匹配队列、发布管理等）

     ---
     Phase 1: 用户与账户管理

     1.1 用户列表

     文件: src/pages/Users.tsx (新建)
     - 表格展示所有注册用户
     - 列：ID、用户名、邮箱、手机、注册时间、最后登录、状态
     - 支持搜索、分页、状态筛选
     - 操作：禁用/启用、查看详情

     1.2 用户详情

     文件: src/pages/UserDetail.tsx (新建)
     - 用户基本信息
     - 关联的 Agent 列表
     - 使用统计（API 调用次数、Token 消耗）
     - 最近的会话记录

     1.3 账户/企业配置

     文件: src/pages/Accounts.tsx (新建)
     - 企业信息管理（名称、行业、规模）
     - Agent 配额管理（最大 Agent 数、Token 限额）
     - 套餐/订阅信息

     ---
     Phase 2: 运营数据分析

     2.1 增强 Dashboard

     文件: src/pages/Dashboard.tsx (增强现有)
     - 新增卡片：今日活跃用户、API 调用趋势
     - Token 消耗趋势图
     - Agent 使用排行（按调用量）
     - 实时在线用户数

     2.2 使用统计报表

     文件: src/pages/UsageReport.tsx (新建)
     - 时间范围筛选（今日/本周/本月/自定义）
     - Token 消耗明细表
     - API 调用统计
     - 按 Agent 类型分类的用量占比
     - 导出 CSV/Excel

     ---
     Phase 3: Agent 业务配置

     3.1 Agent 业务属性配置

     文件: src/pages/AgentConfig.tsx (新建)
     - 基于 FIPAI Agent CRUD，获取 Agent 列表后
     - 业务层扩展配置（不修改 FIPAI API）：
       - 社群助手：社群 ID、欢迎语、自动回复规则
       - 供需匹配：匹配算法参数、推送频率
       - 发布助手：发布平台配置、审核规则
       - 匹配助手：匹配类型、过滤条件
     - 存储在燃冰自己的数据库或前端 localStorage（取决于架构选择）

     3.2 Agent 运营面板

     文件: src/pages/AgentDashboard.tsx (新建)
     - 单个 Agent 的详细运营数据
     - 对话量趋势、用户满意度、响应质量
     - 异常对话告警

     ---
     Phase 4: 业务特定功能

     4.1 社群内容管理 (Community Content)

     文件: src/pages/CommunityContent.tsx (新建)
     - 帖子列表、评论管理
     - 内容审核状态
     - 自动审核结果

     4.2 匹配队列管理 (Matching Queue)

     文件: src/pages/MatchingQueue.tsx (新建)
     - 供需匹配队列
     - 匹配状态：待匹配、已匹配、已确认、已完成
     - 手动干预匹配

     4.3 发布管理 (Publishing)

     文件: src/pages/PublishQueue.tsx (新建)
     - 发布任务队列
     - 发布状态跟踪
     - 失败重试

     ---
     Phase 5: 系统配置

     5.1 通知配置

     文件: src/pages/NotificationConfig.tsx (新建)
     - 邮件/短信模板
     - 告警规则

     5.2 API Key 管理

     文件: src/pages/APIKeys.tsx (新建)
     - 为不同 Agent 生成 API Key
     - 访问权限控制

     ---
     关键文件清单

     ┌────────────────────────────────┬──────┬────────────────┐
     │              文件              │ 操作 │      说明      │
     ├────────────────────────────────┼──────┼────────────────┤
     │ src/pages/Users.tsx            │ 新建 │ 用户列表       │
     ├────────────────────────────────┼──────┼────────────────┤
     │ src/pages/UserDetail.tsx       │ 新建 │ 用户详情       │
     ├────────────────────────────────┼──────┼────────────────┤
     │ src/pages/Accounts.tsx         │ 新建 │ 账户管理       │
     ├────────────────────────────────┼──────┼────────────────┤
     │ src/pages/UsageReport.tsx      │ 新建 │ 使用报表       │
     ├────────────────────────────────┼──────┼────────────────┤
     │ src/pages/Dashboard.tsx        │ 增强 │ 运营数据卡片   │
     ├────────────────────────────────┼──────┼────────────────┤
     │ src/pages/AgentConfig.tsx      │ 新建 │ Agent 业务配置 │
     ├────────────────────────────────┼──────┼────────────────┤
     │ src/pages/AgentDashboard.tsx   │ 新建 │ Agent 运营面板 │
     ├────────────────────────────────┼──────┼────────────────┤
     │ src/pages/CommunityContent.tsx │ 新建 │ 社群内容管理   │
     ├────────────────────────────────┼──────┼────────────────┤
     │ src/pages/MatchingQueue.tsx    │ 新建 │ 匹配队列       │
     ├────────────────────────────────┼──────┼────────────────┤
     │ src/pages/PublishQueue.tsx     │ 新建 │ 发布管理       │
     ├────────────────────────────────┼──────┼────────────────┤
     │ src/utils/api.ts               │ 新增 │ 用户管理 API   │
     └────────────────────────────────┴──────┴────────────────┘

     ---
     验证步骤

     1. cd /Users/wangsheng/fipai-console && npm run dev
     2. 访问 http://localhost:5173
     3. 测试流程：
       - Dashboard 查看运营概览
       - 用户管理 → 查看用户列表
       - 使用报表 → 查看 Token 消耗
       - Agent 配置 → 配置业务属性
       - 业务功能 → 对应模块

     ---
     实施顺序建议

     1. Phase 1 - 用户与账户管理（基础，先做）
     2. Phase 2 - 运营数据分析（数据可视化，重要）
     3. Phase 3 - Agent 业务配置（核心功能）
     4. Phase 4 - 业务特定功能（根据实际需求调整）
     5. Phase 5 - 系统配置（可选）