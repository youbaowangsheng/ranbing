# AI增强功能Spec：供需广场 + 校友社群

> 版本：v1.0 | 日期：2026-05-02 | 状态：设计稿待确认

---

## 一、供需广场AI增强

### 1.1 产品背景与目标

**现状问题：**
- 用户发布供需的门槛高——不知道"我有什么可以拿出来"
- 匹配靠用户主动搜索，效率低
- 发布后缺乏主动推送，等于"石沉大海"

**核心目标：**
1. 用AI降低发布门槛——用户说一段话，AI帮他发现机会
2. 从"被动搜索"变为"AI主动推送匹配"
3. 让撮合过程可见、可用、可控

---

### 1.2 功能模块一：「机会Feed」（K页改造）

**产品定位：** 供需广场的第一个Tab，不再是"列表"，而是"AI为你推送的机会流"

**用户场景：**
> 用户打开供需广场，不是去找，而是在"机会Feed"里看到"有什么可能帮得上或被帮得上"

**交互逻辑：**

```
打开供需广场 → 默认显示「机会」Tab
             ↓
AI主动推送：你可能帮得上忙的人（3-5条）
            + 你可能需要的人（2-3条）
             ↓
每条机会卡片显示：
├── 匹配度%（颜色区分高/中/低）
├── 对方供需摘要
├── AI匹配理由（为什么推荐给你）
├── 共同连接（一二度人脉）
└── 操作：「主动联系」「请AI牵线」
```

**机会卡片类型：**

| 类型 | 触发条件 | 卡片特征 |
|------|---------|---------|
| 正向匹配 | 他人的需求 & 你的供给标签吻合 | 绿色高亮，显示"你能帮上忙" |
| 反向匹配 | 你的需求 & 他人的供给标签吻合 | 蓝色高亮，显示"有人能帮你" |
| 间接匹配 | 标签非直接重合但存在合作可能 | 灰色，显示"间接相关" |

**「AI帮你找」功能：**
- 用户点击顶部入口，进入对话式弹窗
- 可输入任意文字描述需求，AI返回精准匹配结果
- 提供快捷问题按钮（融资帮助/客户资源/渠道合作/品牌曝光）
- 每次查询结果带匹配度%+匹配理由

**「请AI牵线」流程：**
1. 用户点击「请AI牵线」
2. 弹出确认弹窗，显示AI自动生成的牵线话术
3. 话术由AI根据双方标签、供需内容自动生成，用户可修改
4. 发送后，接收方收到AI通知，可选择是否接受

**关键数据指标：**
- 机会卡片点击率（目标 > 35%）
- AI牵线请求发送率（目标 > 15%）
- 牵线后双方互动率（目标 > 40%）

---

### 1.3 功能模块二：「AI引导式发布」（S页改造）

**产品定位：** 3步引导式发布流程，大幅降低用户发布门槛

**核心洞察：**
> 用户不知道自己有价值——AI的职责是帮他发现"我能提供什么"，
> 而不是让他自己填写一个复杂的表单。

**交互流程：**

```
步骤1「AI发现」：用户随便说 → AI分析提取供需
  │
  ├─ 输入框：随便说什么都行
  ├─ 快捷示例：快速填充
  ├─ AI分析：
  │   ├─ 识别是「供给」「需求」还是「都有」
  │   ├─ 推荐标签（用户可调整）
  │   ├─ 生成供需摘要描述
  │   └─ AI曝光优化评分（0-100）
  │
  └─ 继续 → 步骤2

步骤2「确认信息」：AI提取 → 用户确认/调整
  │
  ├─ 显示AI提取的标题/描述/标签
  ├─ 用户可修改
  ├─ 关联Profile标签（AI已提取）
  ├─ 有效期选择
  └─ 隐私设置
       │
       └─ 继续 → 步骤3

步骤3「发布确认」：预览 → 发布
  │
  ├─ 预览卡片样式
  ├─ 发布收益说明（同步Profile / AI推送 / 有效期）
  └─ 确认发布
       │
       └─ 发布成功 → 跳转供需广场「机会」Tab
```

**AI分析引擎的核心能力：**

1. **意图识别**：识别用户描述中的"提供"意图还是"需求"意图，还是两者兼有
2. **标签提取**：从非结构化文字中提取Profile标签词库中的匹配标签（见标签体系文档）
3. **供需映射**：标签 → 供需类型映射，AI自动生成结构化供需描述
4. **质量评分**：综合标签精准度、描述完整度、供需明确度，给出曝光转化预估分
5. **优化建议**：告诉用户怎么改能提升曝光（具体可操作建议，不是废话）

**关键数据指标：**
- AI引导发布成功率（目标 > 60%）
- 从"随便说说"到"发布完成"转化率（目标 > 40%）
- 发布后7天内被匹配次数（目标 > 2次/条）

---

## 二、校友社群AI增强

### 2.1 产品背景与目标

**现状问题：**
- 社群 = 聊天群，信息过载但价值密度低
- 99%的人沉默，1%的人刷屏
- 商业机会在对话里存在，但没人整理、没人发现

**核心目标：**
1. AI实时监听群内商业信号
2. 从"聊天群"进化为"智能商业机会网络"
3. 让"有人在群里提到了你能接住的需求"这件事可见

---

### 2.2 功能模块一：「社群AI信号Feed」

**产品定位：** 社群Tab的第一个子Tab，展示AI实时发现的高价值信号

**核心场景：**

> AI在社群里实时监听——当有人提到你能接住的需求，
> 或者有人问了一个你知道答案的问题，AI主动通知你。

**信号类型：**

| 信号类型 | 触发条件 | 颜色标识 | 推荐动作 |
|---------|---------|---------|---------|
| 供需信号 | 群友提到需求 & 你的Profile/供需标签匹配 | 橙色 | AI牵线/告知对方 |
| 问答信号 | 群友提问 & 群内有对应领域专家 | 蓝色 | 代为询问/推荐专家 |
| 合作信号 | 群友分享业务 & 你可能有合作机会 | 紫色 | 联系/请AI牵线 |

**信号卡片结构：**

```
┌─────────────────────────────────┐
│ [图标] 2015级MBA校友群 · 王海   │
│         今天 14:23 在群里说    [供需信号]
├─────────────────────────────────┤
│ "我们公司下季度要做数字化转型， │
│  正在选供应商..."              │
├─────────────────────────────────┤
│ ✨ AI识别：潜在需求             │
│ [标签] [标签] [标签]           │
│                                 │
│ 💡 AI分析：与你的"企业服务客户  │
│  资源"标签高度匹配，是否需要AI  │
│  帮你穿针引线？                 │
├─────────────────────────────────┤
│ 👤 推荐对接人                   │
│ [头像] 张华  匹配度94%  一度连接│
├─────────────────────────────────┤
│ [AI牵线王海]    [告知张华]      │
└─────────────────────────────────┘
```

**AI穿针引线流程：**
1. 用户点击「AI牵线」
2. 弹出确认弹窗，显示AI生成的牵线话术
3. 话术根据双方Profile标签、群聊上下文自动生成
4. 发送后，接收方收到AI通知+话术摘要，可选"愿意见"/"婉拒"
5. 双方都愿意见 → AI交换联系方式

**AI代为询问流程（Q&A信号）：**
1. 群友提问 → AI识别领域专家
2. 推送给可能知道答案的人：「有人在问X，你在Y领域有经验，要不要帮忙？」
3. 点击"代为询问" → AI以用户名义向专家私发消息
4. 专家回答后 → AI通知提问者，并抄送你

---

### 2.3 功能模块二：「社群精细化推荐」

**在"我的社群"Tab底部：**

```
✨ AI精选：你可能想加入的社群

┌─────────────────────────────────┐
│ [图标] 企业服务创始人闭门群     │
│ 128位 · 企业服务              │
│ 💡 与你的标签高度匹配          │
│ 🔥 本周热议：SaaS续费率打法    │
│ 群内热议摘要...                │
│                                 │
│ [加入群聊]    [先看看]         │
└─────────────────────────────────┘
```

**推荐逻辑：**
- 用户Profile标签 & 供需内容 匹配 社群主题
- 社群活跃度（每周消息数、供需发布数）
- 共同连接数（有多少认识的人在群里）

---

### 2.4 技术实现要点

**1. AI信号识别引擎（后台持续运行）：**

```
群聊消息 → AI实时解析
          ├─ 意图分类：供需？问答？合作？闲聊？
          ├─ 实体提取：公司/人/需求/供给
          ├─ 标签映射：→ Profile标签体系
          ├─ 匹配计算：与所有校友Profile/供需做匹配
          └─ 质量过滤：低于阈值丢弃，高于阈值生成信号

信号分发：
├─ 高匹配（>85%）→ 实时推送AI通知
├─ 中匹配（60-85%）→ 进入机会Feed
└─ 低匹配（<60%）→ 不展示
```

**2. 穿针引线的中介逻辑：**
- 用户A点击牵线 → AI生成话术 → A确认发送
- AI将A的Profile摘要 + 合作意向 发送给B
- B选择"愿意见"/"婉拒"/"先了解"
- 双方同意 → 交换微信/手机（平台中介）

**3. 隐私保护：**
- 群内消息仅AI可见，普通用户看不到他人私聊意图
- 穿针引线需双方同意才能交换联系方式
- 用户可关闭"被AI穿针引线"开关（隐私设置）

---

## 三、数据流转与依赖

```
┌──────────────────────────────────────────────────────┐
│                    用户Profile数据                     │
│  标签 + 供需内容 + 教育背景 + 工作经历 + 社交关系图谱   │
└─────────────────────┬────────────────────────────────┘
                      │
        ┌─────────────┼─────────────┐
        ▼             ▼             ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│  供需广场    │ │  校友社群    │ │  AI人脉助手 │
│  AI匹配引擎 │ │  AI信号监听  │ │              │
└──────┬───────┘ └──────┬───────┘ └──────────────┘
       │                │
       ▼                ▼
┌──────────────┐ ┌──────────────┐
│  机会Feed   │ │  信号Feed   │
│  AI推送     │ │  AI推送     │
└──────┬───────┘ └──────┬───────┘
       │                │
       ▼                ▼
┌──────────────────────────────────┐
│         穿针引线服务               │
│  A意愿 → AI话术 → B确认 → 交换   │
└──────────────────────────────────┘
```

---

## 四、上线节奏建议

| 阶段 | 功能 | 优先级 | 预计工作量 |
|------|------|--------|-----------|
| P0 | AI引导式发布（S页） | 最高 | 中 |
| P0 | 机会Feed（K页） | 最高 | 中 |
| P1 | AI帮你找对话框 | 次高 | 小 |
| P1 | AI牵线确认流程 | 次高 | 小 |
| P2 | 社群AI信号监听 | 中 | 大 |
| P2 | AI代为询问 | 中 | 中 |

---

## 五、关键设计原则

1. **AI的价值要可见**：不是"AI帮你匹配"，而是"AI发现了一个你自己根本找不到的连接"
2. **用户始终有控制权**：AI生成的每个内容（话术、标签、摘要）用户都可以修改
3. **隐私对等**：穿针引线是双向同意的，不会强制曝光任何人
4. **反馈闭环**：每次AI推送后，记录用户行为（点击/忽略/操作），持续优化匹配模型
