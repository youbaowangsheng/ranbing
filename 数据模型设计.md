# 燃冰 · 数据模型设计文档

> 版本：v1.0 · 2026-05-04
> 范围：MVP（用户档案 + 供需匹配 + AI关系维护）

---

## 一、实体关系总图（ER Diagram）

```
users ──1:1── profiles ──N:N── tags (via profile_tags)
              │
              ├── supplies ──N:N── tags (via tags[])
              │           └── supply_embeddings (vector)
              │
              ├── profile_embeddings (vector)
              │
              ├── connections (N:N between users)
              │
              ├── followups
              │
              ├── activity_enrollments ──1:N── activities
              │
              └── community_members ──1:N── communities
                                      └── messages ──N:1── profiles
```

---

## 二、表结构详解

### 2.1 users — 用户表

**职责：** 用户账户层，微信openid/手机号的容器

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGSERIAL | PK | 自增主键 |
| uuid | UUID | UNIQUE | 对外暴露的ID |
| phone | VARCHAR(20) | UNIQUE | 手机号 |
| wx_openid | VARCHAR(128) | UNIQUE | 微信openid |
| wx_unionid | VARCHAR(128) | UNIQUE | 微信unionid |
| nickname | VARCHAR(64) | - | 昵称 |
| avatar_url | VARCHAR(512) | - | 头像URL |
| status | SMALLINT | DEFAULT 1 | 1=正常 2=封禁 9=注销 |
| is_verified | BOOLEAN | DEFAULT FALSE | 是否验证 |
| last_login_at | TIMESTAMPTZ | - | 最近登录时间 |
| created_at | TIMESTAMPTZ | DEFAULT NOW() | 创建时间 |

**索引：**
- `idx_users_phone` — 手机号查询（唯一索引）
- `idx_users_wx_openid` — 微信登录
- `idx_users_status` — 状态筛选

**读写频率：** 写高（每次登录更新last_login_at），读低

---

### 2.2 profiles — 校友档案表

**职责：** 用户画像层，承载校友的真实信息

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| user_id | BIGINT | FK→users, UNIQUE | 关联用户 |
| real_name | VARCHAR(64) | NOT NULL | 真实姓名 |
| gender | SMALLINT | - | 1=男 2=女 0=未知 |
| birthday | DATE | - | 生日 |
| company | VARCHAR(128) | - | 公司 |
| position | VARCHAR(128) | - | 职位 |
| industry | VARCHAR(64) | - | 行业 |
| city | VARCHAR(64) | - | 所在城市 |
| bio | TEXT | - | 个人简介 |
| education_year | VARCHAR(10) | - | 入学年份 |
| education_major | VARCHAR(128) | - | 专业 |
| education_school | VARCHAR(128) | - | 学校 |
| cert_level | SMALLINT | DEFAULT 0 | 认证等级 0~3 |
| cert_status | SMALLINT | DEFAULT 0 | 认证状态 |
| conn_count | INT | DEFAULT 0 | 关系数量（冗余） |
| active_score | DECIMAL(6,2) | DEFAULT 0 | 活跃度评分 |
| last_active_at | TIMESTAMPTZ | - | 最近活跃时间 |

**认证等级说明：**
- 0 = 未认证
- 1 = 手机认证（手机号验证）
- 2 = 校友认证（入学年份+学校+专业）
- 3 = 深度认证（名片/工牌/证明文件）

**索引：**
- `idx_profiles_cert_level` — 按认证等级筛选高价值用户
- `idx_profiles_industry` — 按行业查找
- `idx_profiles_school` — 按学校查找
- `idx_profiles_active` — 活跃度排序

**读写频率：** 读高（AI匹配、推荐、搜索），写中（编辑时）

---

### 2.3 tags — 标签表

**职责：** 五层标签体系，支撑供需匹配和AI推荐

**L1~L5层级说明：**

```
L1（行业大类）  →  L2（资源类型）  →  L3（具体标签）  →  L4（属性）  →  L5（细节）
──────────────────────────────────────────────────────────────────────────────
供给资源        →  资金资源        →  天使投资          →  偏好阶段  →  种子轮
                →  人脉资源        →  行业专家          →  擅长领域  →  医疗健康
                →  渠道资源        →  销售渠道          →  覆盖区域  →  长三角
                →  技术资源        →  技术团队          →  技术栈    →  AI/ML
需求帮助        →  找资金          →  创业融资          →  需求阶段  →  天使轮
                →  找人脉          →  行业专家对接      →  行业      →  教育
                →  找渠道          →  销售渠道拓展      →  区域      →  全国
                →  找技术          →  技术合伙人        →  类型      →  全栈
行业经验        →  咨询顾问        →  战略咨询          →  行业      →  消费
                →  培训分享        →  内训服务          →  形式      →  线上
投资意向        →  项目推荐        →  优质早期项目      →  轮次      →  Pre-A
                →  投资机会        →  大股东股权转让    →  金额      →  500万内
社交兴趣        →  活动参与        →  行业沙龙          →  频率      →  每月
                →  社群加入        →  主题社群          →  类型      →  创业
```

**索引：**
- `idx_tags_category` — L1+L2 联合索引，AI快速筛选候选标签
- `idx_tags_type` — 1=供给 2=需求 3=通用
- `idx_tags_hot` — 推荐热度排序

---

### 2.4 profile_tags — 用户-标签关联表

**职责：** 用户自选标签，AI扩展标签

| 字段 | 类型 | 说明 |
|------|------|------|
| profile_id | FK→profiles | 用户档案 |
| tag_id | FK→tags | 标签 |
| tag_type | SMALLINT | 1=供给标签 2=需求标签 |
| weight | DECIMAL(3,2) | 权重 0.5~1.0 |
| is_ai_ext | BOOLEAN | 是否为AI推荐后用户确认 |

**设计说明：**
- 权重支持用户手动调整重要程度
- `is_ai_ext=TRUE` 的标签会标注"AI推荐"角标，增加用户信任

---

### 2.5 supplies — 供需表

**职责：** 供需广场的核心数据，用户发布的供给/需求

| 字段 | 类型 | 说明 |
|------|------|------|
| supply_type | SMALLINT | 1=供给 2=需求 |
| title | VARCHAR(256) | 标题（AI生成/用户填写） |
| content | TEXT | 详细描述 |
| tags | BIGINT[] | 标签ID数组（GIN索引） |
| match_count | INT | 被AI匹配次数 |
| view_count | INT | 被浏览次数 |
| status | SMALLINT | 1=有效 2=已下架 3=已成交 |
| quality_score | DECIMAL(4,2) | AI质量评分（0~100） |
| expires_at | TIMESTAMPTZ | 过期时间（默认30天） |

**GIN索引说明：**
- `USING GIN(tags)` 支持 `&&` 数组包含查询
- 典型查询：`SELECT * FROM supplies WHERE tags && ARRAY[5,12]`

---

### 2.6 profile_embeddings — 用户向量表

**职责：** 用户画像的语义向量，用于AI相似度匹配

| 字段 | 类型 | 说明 |
|------|------|------|
| embedding | VECTOR(1536) | text-embedding-3-small 向量 |
| model_name | VARCHAR(64) | 向量模型名称 |
| updated_at | TIMESTAMPTZ | 更新时间 |

**IVFFlat索引说明：**
- `USING ivfflat(embedding vector_cosine_ops)` — 余弦相似度索引
- 构建方式：`SET ivfflat.probes = 10`（召回率 vs 性能平衡）

**更新策略：**
- 用户编辑Profile后异步更新（Celery任务）
- 每天凌晨批量更新高活跃用户的向量

---

### 2.7 matches — AI匹配记录表

**职责：** AI匹配引擎的核心输出，记录所有AI推荐的供需/人脉配对

| 字段 | 类型 | 说明 |
|------|------|------|
| supply_id | FK→supplies | 匹配的供需ID |
| target_profile_id | FK→profiles | 被推荐的用户ID |
| match_score | DECIMAL(5,4) | 匹配分 0.0000~1.0000 |
| ai_reason | TEXT | AI匹配理由 |
| match_type | SMALLINT | 1=供需匹配 2=人脉推荐 3=活动推荐 |
| status | SMALLINT | 1=待处理 2=已联系 3=已成交 4=已忽略 |
| push_status | SMALLINT | 推送状态（追踪转化漏斗） |
| feedback_score | SMALLINT | 用户反馈 1=准确 2=一般 3=不匹配 |

**推送漏斗：**
```
推送 → 已点击 → 已联系 → 已成交
  ↓       ↓        ↓        ↓
         反馈评分    反馈评分   反馈评分
```

---

### 2.8 connections — 关系链表

**职责：** 用户间的关系网络，记录所有社交连接

**特殊设计：**
- `CHECK(user_a_id < user_b_id)` — 保证每对关系只有一条记录
- `is_mutual` — 是否双向确认（单向关注/双向好友）
- `relation_strength` — 关系强度（0.0~1.0），AI根据互动频率计算

**关系类型（conn_type）：**
- 1=好友（双向确认）
- 2=粉丝（单向关注）
- 3=校友（同一学校）
- 4=活动认识（共同参加活动）
- 5=供需认识（通过匹配认识）

---

### 2.9 followups — AI跟进记录表

**职责：** AI主动跟进的工作流，记录AI生成的话术和执行状态

| 字段 | 类型 | 说明 |
|------|------|------|
| trigger_event | SMALLINT | 触发事件类型 |
| ai_script | TEXT | AI生成的话术 |
| followup_type | SMALLINT | 1=AI建议 2=已发送 3=已回复 4=已添加微信 |
| scheduled_at | TIMESTAMPTZ | 计划发送时间 |
| sent_at | TIMESTAMPTZ | 实际发送时间 |
| result | SMALLINT | 1=positive 2=neutral 3=negative |

**触发事件：**
- 1=刚匹配（AI推送后24小时）
- 2=活动结束（活动结束后3天）
- 3=社群互动（在群里有供需信号时）
- 4=自定义（用户手动触发）

---

### 2.10 activities — 社交活动表

**职责：** 活动管理和AI推荐

| 字段 | 类型 | 说明 |
|------|------|------|
| activity_type | SMALLINT | 1=沙龙 2=路演 3=培训班 4=社交聚会 5=线上讲座 |
| organizer_id | FK→profiles | 主办人 |
| host_school | VARCHAR(128) | 主办学校 |
| location + lat/lng | VARCHAR+DECIMAL | 地点+经纬度（用于附近活动推荐） |
| enrollment_mode | SMALLINT | 1=免费 2=付费 3=审核 |
| ai_match_enabled | BOOLEAN | 是否开启AI推荐参与者 |
| tags | BIGINT[] | 活动标签（用于AI匹配） |

---

### 2.11 communities — 校友社群表

**职责：** 校友社群元数据

| 字段 | 类型 | 说明 |
|------|------|------|
| community_type | SMALLINT | 1=行业社群 2=地域社群 3=校友群 4=兴趣社群 |
| school | VARCHAR(128) | 关联学校 |
| owner_id | FK→profiles | 群主 |
| qr_code_url | VARCHAR(512) | 微信群二维码URL |

**设计说明：**
- 不存储微信群消息（合规要求）
- 群二维码定期更新，到期后管理员重新上传

---

### 2.12 messages — 社群消息表

**职责：** 记录用户主动发布到平台的内容（不含微信群消息）

| 字段 | 类型 | 说明 |
|------|------|------|
| ai_signal_type | SMALLINT | AI识别的信号类型 |
| ai_signal_extracted | BOOLEAN | 是否已提取信号 |

**AI信号类型：**
- 1=供需信号（"我有XXX资源"）
- 2=问答信号（"求助：哪里找XXX"）
- 3=合作意向（"有没有人想一起做XXX"）
- 4=资源推介（"推荐XXX"）

**设计说明：**
- 用户在App内分享内容时触发AI分析
- 微信群消息通过Webhooks接入（如有）

---

### 2.13 ai_conversations / ai_messages — AI对话表

**职责：** 支持多轮对话的上下文记忆

| 字段 | 类型 | 说明 |
|------|------|------|
| context_summary | TEXT | AI生成的对话摘要（用于跨会话记忆） |
| channel | SMALLINT | 1=AI助手(Q) 2=AI发布引导(S) 3=AI匹配反馈(K) |

---

## 三、索引设计策略

### 3.1 向量索引（pgvector）

```sql
-- 用户向量：余弦相似度检索
CREATE INDEX idx_profile_emb_cosine
    ON profile_embeddings USING ivfflat(embedding vector_cosine_ops)
    WITH (lists = 100);

-- 供需向量：余弦相似度检索
CREATE INDEX idx_supply_emb_cosine
    ON supply_embeddings USING ivfflat(embedding vector_cosine_ops)
    WITH (lists = 100);
```

### 3.2 B-Tree 索引

| 索引名 | 表 | 字段 | 用途 |
|--------|-----|------|------|
| idx_supplies_type_status | supplies | (supply_type, status) | 广场列表筛选 |
| idx_matches_score | matches | match_score DESC | Top-K推荐 |
| idx_profiles_active | profiles | active_score DESC | 活跃用户推荐 |
| idx_supplies_created | supplies | created_at DESC | 广场按时间排序 |

### 3.3 GIN 索引

```sql
-- 标签数组包含查询
CREATE INDEX idx_supplies_tags ON supplies USING GIN(tags);
-- 查询示例：SELECT * FROM supplies WHERE tags && ARRAY[5, 12]
```

---

## 四、数据流转图

```
用户输入
   ↓
┌─────────────┐     ┌─────────────────┐     ┌──────────────┐
│ Profile信息  │ ──→ │ text-embedding  │ ──→ │ profile_emb │
│ (姓名/公司/  │     │   (DeepSeek)    │     │  (向量)      │
│  行业/标签)  │     └─────────────────┘     └──────────────┘
└─────────────┘
   ↓
┌─────────────┐     ┌─────────────────┐     ┌──────────────┐
│ Supply信息   │ ──→ │ text-embedding  │ ──→ │ supply_emb   │
│ (标题/内容/  │     │   (DeepSeek)    │     │  (向量)      │
│  标签)       │     └─────────────────┘     └──────────────┘
└─────────────┘
   ↓
┌────────────────────────────────────────────────────────────┐
│                    AI Match Engine                        │
│   cosine_sim(profile_emb, supply_emb)  →  match_score     │
│   + tag_overlap(supply.tags, profile.tags) → tag_score    │
│   + cert_level_boost → cert_bonus                             │
│   ─────────────────────────────────────────                 │
│   total_score = weighted(sum) → Top-K → ai_reason          │
└────────────────────────────────────────────────────────────┘
   ↓
┌─────────────┐
│  matches表   │ → AI推送 → 用户反馈 → followups表
└─────────────┘
```

---

## 五、扩展性考虑

### 5.1 分库分表（远期）

- `supplies` 表按 `profile_id % N` 分片
- `messages` 表按 `community_id % N` 分片
- `profile_embeddings` 跟随 profiles 表

### 5.2 冷热数据分离

- `connections` 表超过1年的记录归档到历史表
- `ai_messages` 表超过30天的记录压缩/归档

### 5.3 缓存策略

- 热门Profile数据：Redis SET，TTL=10min
- 标签树：Redis HASH，TTL=1hour
- AI推荐结果：Redis LIST，TTL=30min
